package LCS;

public class MiniNoOfDeletionToMakeStringPalindrome {
    /*
    String "s"
    let's say, after deletion, we found palindrome "p"

    #del = len(s)-len(p);
    to minimize #del, we need to maximize len(p) ==> longest palindromic subsequence

     */
}
